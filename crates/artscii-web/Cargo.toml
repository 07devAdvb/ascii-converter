[package]
name = "artscii-web"
version = "2.0.0"
edition = "2024"
authors = ["4ster-light"]
description = "Web interface for ArtSCII - convert images to ASCII art in the browser"
license = "MIT"
repository = "https://github.com/4ster-light/artscii"

[dependencies]
# ArtSCII core functionality
artscii-core = { path = "../artscii-core" }
artscii-img = { path = "../artscii-img", default-features = false }

# Leptos for reactive web UI
leptos = { version = "0.8", features = ["csr"] }

# Image processing
image = "0.25"

# WASM utilities
wasm-bindgen = "0.2"
wasm-bindgen-futures = "0.4"
js-sys = "0.3"
web-sys = { version = "0.3", features = [
    "Blob",
    "BlobPropertyBag",
    "Clipboard",
    "DataTransfer",
    "Document",
    "DragEvent",
    "Event",
    "File",
    "FileList",
    "HtmlElement",
    "HtmlInputElement",
    "Navigator",
    "Url",
    "Window",
] }

# Utilities
console_error_panic_hook = "0.1"
